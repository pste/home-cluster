apiVersion: v1
kind: Secret
metadata:
  name: tailscale-auth
  namespace: tailscale
type: Opaque
stringData:
  # Placeholder — replace with your real auth key after applying:
  #   kubectl create secret generic tailscale-auth \
  #     --from-literal=TS_AUTHKEY=tskey-auth-XXXXXXXXX \
  #     --namespace tailscale --dry-run=client -o yaml | kubectl apply -f -
  TS_AUTHKEY: helloworld
