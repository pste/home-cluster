apiVersion: v1
kind: Secret
metadata:
  name: cloudflare-api-token
  namespace: cert-manager
type: Opaque
stringData:
  # Real value substituted from .env (CFTOK) via envsubst at apply time
  api-token: ${CFTOK}
