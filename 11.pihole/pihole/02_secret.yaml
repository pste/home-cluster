apiVersion: v1
kind: Secret
metadata:
  name: pihole-webpassword
  namespace: pihole
type: Opaque
stringData:
  # Real value substituted from .env (PIHOLE_WEBPASSWORD) via envsubst at apply time
  WEBPASSWORD: ${PIHOLE_WEBPASSWORD}
